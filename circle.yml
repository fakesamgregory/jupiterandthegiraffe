deployment:
  staging:
    branch: dev
    codedeploy:
      jupiterandthegiraffe:
        application_root: /
        region: us-west-1
        revision_location:
          revision_type: S3
          s3_location:
            bucket: jupiterandthegiraffe
            key_pattern: jupiterandthegiraffe-{BRANCH}-{SHORT_COMMIT}
