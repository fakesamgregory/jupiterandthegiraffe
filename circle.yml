deployment:
  production: # just a label; label names are completely up to you
    branch: master
    commands:
      - npm run build
      - scp -rp ~/dist/* sam@178.62.126.206:/var/www/jupiterandthegiraffe/prod/public_html
  development:
      branch: dev  # branches starting with "feature-"
      commands:
        - npm run build
        - scp -rp ~/dist/* sam@178.62.126.206:/var/www/jupiterandthegiraffe/dev/public_html

