---
machine:
  node:
    version: 8.2.1

deployment:
  production:
    branch: master
    commands:
      - bash ./deploy.sh
  development:
    branch: dev
    commands:
      - bash ./deploy.sh

dependencies:
  override:
    - npm rebuild node-sass
    - npm install

test:
  override:
    - echo 'test'

