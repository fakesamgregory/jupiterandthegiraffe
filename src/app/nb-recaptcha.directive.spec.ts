import { NbRecaptchaDirective } from './nb-recaptcha.directive';

describe('NbRecaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new NbRecaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
